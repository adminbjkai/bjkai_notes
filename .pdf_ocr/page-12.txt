Your Turn to Build: Getting Started
Locally.

Running Docmost locally is simple. Clone the repository, configure your
environment, and launch the stack with Docker Compose.

1. Configure Environment 2. Install Dependencies 3. Launch & Build
Copy .env.example to .env and Access the application at
fill in the required secrets. pnpm install http://Localhost: 3000.
APP_URL=http: //LocaLhost: 3000 docker-compose up -d
DATABASE_URL="postgresql://do pnpm build
cmost: STRONG_DB_PASSWORD pnpm start

_PASSWORD@db :5432/docmost?
schema=pubLic"
REDIS_URL="redis://redis:6379"

A) NotebookLM
